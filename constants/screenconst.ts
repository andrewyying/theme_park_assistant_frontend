// Layout-related constants. Screen size etc.
import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

export const ScreenConst = {
  window: {
    width,
    height,
  }
};
